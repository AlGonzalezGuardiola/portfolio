{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "Portfolio (Python HTTP Server)",
      "runtimeExecutable": "python",
      "runtimeArgs": ["-c", "import os,http.server,functools; http.server.test(HandlerClass=functools.partial(http.server.SimpleHTTPRequestHandler, directory='.'), port=int(os.environ.get('PORT', 8080)), bind='127.0.0.1')"],
      "port": 8080,
      "autoPort": true
    }
  ]
}
